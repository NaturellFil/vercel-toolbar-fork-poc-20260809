# Vercel Toolbar fork delivery fixture

This repository contains a researcher-owned synthetic fixture for authorized
testing in Vercel's private HackerOne program.

The `main` branch is the victim Production fixture. A separate owned GitHub
identity will create a fork pull request for the Preview delivery test.

